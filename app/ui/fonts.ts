import { Inter, Sour_Gummy, Lusitana } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const sourGummy = Sour_Gummy({
  weight: ["400", "700"],
  subsets: ["latin"],
});

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
  });